syntax = "proto3";

import "elixirpb.proto";

option (elixirpb.file).module_prefix = "Crane.Protos.Browser.Window.History";
option swift_prefix = "Crane";

message Frame {
  map<string, string> state = 1;
  string url = 2;
}
